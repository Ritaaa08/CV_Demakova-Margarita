# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Claude Code skill/config (not part of the site)
.claude/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Raw source materials (already exported into public/ where the site uses them)
# Leading slash = root only, so it does NOT also match public/projets (macOS is case-insensitive)
/Projets/
/Monsite(en constr.)/
